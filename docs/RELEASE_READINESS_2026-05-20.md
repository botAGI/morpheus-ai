# Morpheus Release Readiness Report - 2026-05-20

## Current Stage

Morpheus is a published `v0.2.0b1` beta for the source-grounded truth-layer path
and a working local ML-core lab on Apple Silicon.

The correct public framing is:

```text
First verify. Then learn.
```

Morpheus verifies project claims against local source-backed state first. The
learning core is downstream: only strict accepted source-backed candidates may
become training examples, and adapter output is not the source of truth.

## Tested Capabilities

| Capability | Current result |
| --- | --- |
| Deterministic WAKE compile | `morpheus wake . --private` compiled current project state and signed a receipt |
| Receipt verification | `morpheus verify --all` verified the receipt chain |
| Test suite | `pytest tests/ -q` passed 678 tests |
| Lint | `ruff check .` passed |
| Local stale check | stale fixture exits 1 and reports the stale claim |
| Local verified check | correct fixture exits 0 and verifies the claim |
| Learning status | latest lab is `ML_CORE_PASS`, `production_ready=True`, active adapter is `none` |
| Effective trainable dataset | latest lab dataset is selected with 290 examples when standalone dataset is empty |
| Dry-run training plan | `morpheus learn train . --dry-run` uses the latest lab dataset and creates training artifacts |
| Legacy training safety | root `morpheus train --execute` is blocked without explicit unsafe confirmation |
| Daily lab script | `scripts/daily_training.sh` runs wake/verify/lab no-train/status and does not train |
| MCP truth tools | live local `/mcp` smoke passed for tools/list, check, active state, evidence lookup, and WAKE fetch |
| Live MLX dogfood lab | repeat-2 stability passed with 69 strict accepted candidates, 290 examples, 148/148 eval coverage per run |
| Package build | `make build` produced wheel/sdist and `twine check dist/*` passed |

## Latest Live ML Core Result

- Stability ID: `stability_20260520T161542494930Z`
- Stability output:
  `.morpheus/lab/live_runs/dogfood_mlx_stability_fixed_20260520T161542Z.json`
- Repeat: `2`
- Stability verdict: `ML_CORE_PASS`
- Stability blockers: `[]`

Run 1:

- Lab ID: `lab_20260520T161542495419Z`
- Model: `mlx-community/Qwen2.5-7B-Instruct-4bit`
- Adapter path:
  `.morpheus/lab/lab_20260520T161542495419Z/training/adapter`
- Strict accepted candidates: `69`
- Training examples: `290`
- Eval seed items: `75`
- Held-out eval items: `73`
- Base pass rate: `0.7973`
- Adapter pass rate: `0.9932`
- Delta: `+0.1959`
- Regression count: `0`
- Critical failures: `0`
- Adapter hallucination rate: `0.0`
- Verdict: `ML_CORE_PASS`
- Production blockers: `[]`

Run 2:

- Lab ID: `lab_20260520T163443316809Z`
- Base pass rate: `0.7973`
- Adapter pass rate: `0.9932`
- Delta: `+0.1959`
- Regression count: `0`
- Critical failures: `0`
- Adapter hallucination rate: `0.0`
- Verdict: `ML_CORE_PASS`
- Production blockers: `[]`

The adapter was not activated automatically.

## Published Release

Published beta:

- version: `v0.2.0b1`
- package version: `0.2.0b1`
- release notes: `docs/release-notes/v0.2.0b1.md`
- GitHub Release: `https://github.com/botAGI/morpheus-ai/releases/tag/v0.2.0b1`
- PyPI: `https://pypi.org/project/morpheus-wake/`

Post-publish smoke:

- `uvx --from 'morpheus-wake==0.2.0b1' morpheus --version` reports
  `Morpheus AI v0.2.0b1`.
- `uvx --from 'morpheus-wake==0.2.0b1' morpheus wake . --private` passes.
- `uvx --from 'morpheus-wake==0.2.0b1' morpheus verify --all` passes.

Do not activate adapters from this report alone.

## Repository Garbage Audit

Tracked source is clean of generated bytecode, `.morpheus/` runtime artifacts,
local assistant memory files, and build artifacts.

Ignored local artifacts observed during this pass:

- `.morpheus/` live receipts, lab reports, datasets, and adapters,
- `.venv/`,
- `.pytest_cache/`,
- `.ruff_cache/`,
- `dist/` from the previous package build,
- Python `__pycache__/` directories,
- local ignored assistant files `IDENTITY.md` and `SOUL.md`.

Safe cleanup target:

```bash
rm -rf .pytest_cache .ruff_cache dist build
find . -maxdepth 1 -name '*.egg-info' -exec rm -rf {} +
find morpheus tests -type d -name __pycache__ -prune -exec rm -rf {} +
rm -rf tests/fixtures/learning_project/.morpheus/training
```

Do not delete `.morpheus/` during ML-core work; it contains the latest live lab
evidence and adapter artifacts. Do not delete `.venv/` unless rebuilding the
environment deliberately.
